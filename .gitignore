.DS_Store
#No node Modules for us
node_modules
#Ignore our public directory as it can just simply be built on dev container startup
public

#Ignore local docker-compose.override.yml config
docker-compose.override.yml
